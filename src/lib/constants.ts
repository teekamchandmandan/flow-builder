export const DEFAULT_NODE_WIDTH = 200;
export const DEFAULT_NODE_HEIGHT = 80;
export const SNAP_GRID: [number, number] = [15, 15];
export const MAX_HISTORY_SIZE = 50;

export const DEFAULT_NEW_NODE_POSITION_OFFSET = {
  x: 40,
  y: 40,
} as const;
